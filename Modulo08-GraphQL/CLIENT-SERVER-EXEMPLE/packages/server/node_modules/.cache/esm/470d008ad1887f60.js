let express,ApolloServer,gql,typeDefs;_d18‍.w("express",[["default",["express"],function(v){express=v}]]);_d18‍.w("apollo-server-express",[["ApolloServer",["ApolloServer"],function(v){ApolloServer=v}],["gql",["gql"],function(v){gql=v}]]);_d18‍.w("./graphql/typeDefs",[["default",["typeDefs"],function(v){typeDefs=v}]]);




const app = express();

const server = new ApolloServer({
   typeDefs,
   resolvers: {
      Query:{
          demands: () => [],
      },
   },
});

server.applyMiddleware({
    app,
    cors: {
        origin: 'http://localhost:3000',
    },
})


/*
server.use(cors());

server.get('/status', (_, response)=>{
    response.send({
        status:'Okay',
    });
});

const enableCors = cors({
    origin: 'http://localhost:3000'
});

server
    .options('/authenticate', enableCors)
    .post('/authenticate', enableCors , express.json(), (request, response) =>{
        console.log(
            'Email', request.body.email,'Senha', request.body.password);
        response.send({
            Okay: true,
        });
});
*/
const PORT = process.env.PORT? parseInt(process.env.PORT) : 8000;
const HOSTNAME = process.env.HOSTNAME || '127.0.0.1';

app.listen(8000, HOSTNAME, () => {
    console.log(`Server is listening at http://${HOSTNAME}:${PORT}`);
});